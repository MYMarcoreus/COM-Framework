# DataHub 数据传输服务配置（ServerCore Config 模块读取）
# 运行服务器时从当前目录读取本文件（datahub.ini）

[server]
port = 8888

[web]
# 前端页面路径（相对进程工作目录；独立资源文件，与 C++ 代码分离）。
# 若该路径不可访问，将自动基于可执行文件位置（/proc/self/exe）定位到
# DataHub/Web/index.html，保证从任意工作目录启动都能加载。
index = Web/index.html

[log]
level = info
file = datahub.log
